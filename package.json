{
  "name": "telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot for Lua scripts",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "workergram": "0.5.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
